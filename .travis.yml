language: php

php:
  - 7.1
  - 7.2

cache:
  directories:
    - $HOME/.composer/cache

before_install:
  - ./tests/setup.sh

# TODO: reintroduce composer validate after phan 0.10.0 is released
install:
  - composer --prefer-dist install

script:
  - ./test

branches:
  only:
  - master
